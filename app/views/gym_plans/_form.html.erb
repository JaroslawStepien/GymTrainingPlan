<%= form_with(model: gym_plan) do |form| %>
  <% if gym_plan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gym_plan.errors.count, "error") %> prohibited this gym_plan from being saved:</h2>

      <ul>
        <% gym_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="card text-center">
  <div class="card-header">
    <h4>Gym plan</h4>
  </div>

  <div class="card-body">
    <div class="form-label">
      <h5 class="card-title"> <%= form.label :name, style: "display: block" %> </h5>
      <p class="card-text"> <%= form.text_field :name, class: "form-control" %></p>
    </div>
    <div class="form-label">
      <h5 class="card-title"><%= form.label :number_of_days, style: "display: block" %> </h5>
      <p class="card-text"> <%= form.number_field :number_of_days, class: "form-control" %></p>
    </div>
  </div>
</div>
<p>
  <div class="d-flex justify-content-center">
    <%= form.submit class: "btn btn-primary" %>
  </div>
</p>
<% end %>
